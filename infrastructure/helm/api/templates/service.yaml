apiVersion: v1
kind: Service
metadata:
  name: {{ include "tres-crm-api.fullname" . }}
  labels:
    {{- include "tres-crm-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tres-crm-api.selectorLabels" . | nindent 4 }}
