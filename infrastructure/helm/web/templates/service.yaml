apiVersion: v1
kind: Service
metadata:
  name: {{ include "tres-crm-web.fullname" . }}
  labels:
    {{- include "tres-crm-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tres-crm-web.selectorLabels" . | nindent 4 }}
